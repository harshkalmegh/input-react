import React from "react";
import ReactDOM from "react-dom";
import Input from "./Features/Index";

ReactDOM.render(
  <React.StrictMode>
    <Input />
  </React.StrictMode>,
  document.getElementById("root")
);
